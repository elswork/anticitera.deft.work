---
layout: layouts/home.njk
eleventyNavigation:
  key: Inicio
  order: 1
numberOfLatestPostsToShow: 3
lang: "es"
---

<style>
.genesis-banner {
  background: rgba(0, 20, 40, 0.8);
  border: 1px solid #00ffcc;
  padding: 1.5rem;
  margin-bottom: 2rem;
  border-radius: 4px;
  color: #00ffcc;
  font-family: 'Courier New', monospace;
  box-shadow: 0 0 10px rgba(0, 255, 204, 0.2);
}
.genesis-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 1rem;
}
.genesis-title {
  font-weight: bold;
  text-transform: uppercase;
  letter-spacing: 2px;
}
.genesis-status {
  background: #003344;
  padding: 0.2rem 0.5rem;
  font-size: 0.8em;
  border-radius: 2px;
}
.progress-container {
  background: #002233;
  height: 20px;
  width: 100%;
  border: 1px solid #005566;
  position: relative;
  overflow: hidden;
}
.progress-bar {
  background: repeating-linear-gradient(
    45deg,
    #00ccaa,
    #00ccaa 10px,
    #00ffcc 10px,
    #00ffcc 20px
  );
  height: 100%;
  width: 15%; /* Progression: Phase 0 */
  animation: pulse 2s infinite;
  box-shadow: 0 0 10px #00ffcc;
}
@keyframes pulse {
  0% { opacity: 0.8; }
  50% { opacity: 1; }
  100% { opacity: 0.8; }
}
.genesis-info {
  margin-top: 1rem;
  font-size: 0.9em;
  opacity: 0.9;
}
.genesis-actions {
  margin-top: 1rem;
  display: flex;
  gap: 1rem;
}
.action-btn {
  display: inline-block;
  padding: 0.5rem 1rem;
  border: 1px solid #00ffcc;
  color: #00ffcc;
  text-decoration: none;
  font-weight: bold;
  transition: all 0.3s;
}
.action-btn:hover {
  background: #00ffcc;
  color: #000;
  box-shadow: 0 0 15px #00ffcc;
}
</style>

<div class="genesis-banner">
  <div class="genesis-header">
    <span class="genesis-title">:: SYSTEM STATUS ::</span>
    <span class="genesis-status">ONLINE // PHASE 0</span>
  </div>
  
  <div class="progress-container">
    <div class="progress-bar"></div>
  </div>

  <div class="genesis-info">
    <p>> ESTADO: GÉNESIS (Iniciando Nodos Fundadores)</p>
    <p>> INTEGRIDAD: 15% Carga de Identidad</p>
  </div>

  <div class="genesis-actions">
    <a href="/manifiesto/" class="action-btn">LEER MANIFIESTO</a>
    <a href="/ciudadania/" class="action-btn">SOLICITAR CIUDADANÍA</a>
  </div>
</div>

{% set postsCount = collections.posts_by_lang[lang] | length %}
{% set latestPostsCount = postsCount | min(numberOfLatestPostsToShow) %}
<h1>Última{% if latestPostsCount != 1 %}s{% endif %} {{ latestPostsCount }} Publicacion{% if latestPostsCount != 1 %}es{% endif %}</h1>

{% set postslist = collections.posts_by_lang[lang] | head(-1 * numberOfLatestPostsToShow) %}
{% set postslistCounter = postsCount %}
{% include "postslist.njk" %}

{% set morePosts = postsCount - numberOfLatestPostsToShow %}
{% if morePosts > 0 %}
<p>{{ morePosts }} más post{% if morePosts != 1 %}s{% endif %} pueden ser encontrados en <a href="/blog/">el archivo</a>.</p>
{% endif %}

{# List every content page in the project #}
{#
<ul>
	{%- for entry in collections.all %}
	<li><a href="{{ entry.url }}"><code>{{ entry.url }}</code></a></li>
	{%- endfor %}
</ul>
#}
