---
permalink: /llms-full.txt
eleventyExcludeFromCollections: true
---
# Contenido Completo - Proyecto Anticitera

Este archivo contiene la consolidación de los documentos principales del Proyecto Anticitera para facilitar su procesamiento por agentes de IA.

{% set main_pages = [
    "/es/manifiesto/",
    "/es/about/",
    "/es/ciudadania/"
] %}

{%- for path in main_pages -%}
    {%- for item in collections.all -%}
        {%- if item.url == path -%}
---
## DOCUMENTO: {{ item.data.title }}
URL: https://anticitera.deft.work{% if item.url == '/' %}/index.md{% else %}{{ item.url | replace(r/\/$/, '') }}.md{% endif %}

{{ item.content | safe }}

        {%- endif -%}
    {%- endfor -%}
{%- endfor -%}

## BLOG RECIENTE

{%- for post in collections.posts | reverse | head(10) -%}
    {%- if post.data.lang == 'es' or not post.data.lang -%}
---
### POST: {{ post.data.title }}
Fecha: {{ post.date | date }}
URL: https://anticitera.deft.work{{ post.url | replace(r/\/$/, '') }}.md

{{ post.content | safe }}

    {%- endif -%}
{%- endfor -%}
